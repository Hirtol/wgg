query getAggregateIngredient($id: Int!, $price: PriceFilter!) {
    aggregateIngredient(id: $id) {
        ...AggregateCardFragment
        ingredients {
            ...ProductCardFragment
        }
    }
}